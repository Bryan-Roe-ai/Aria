name: "Aria CodeQL — maximum security coverage"

# Use default queries in addition to custom suites to keep Copilot Autofix coverage broad.
disable-default-queries: false

# Keep suites explicit and stable.
queries:
    - uses: security-extended
    - uses: security-and-quality
    - uses: security-experimental

# Optional: remove packs unless you have a specific custom-pack strategy.
# queries suites already include the standard query packs for analyzed languages.

# Language-specific packs for deep analysis
packs:
    - codeql/javascript-queries
    - codeql/python-queries
    - codeql/cpp-queries
    - codeql/actions-queries

# Exclude only generated/vendor/build artifacts.
# Avoid excluding broad source directories to keep coverage high.
paths-ignore:
    # Dependencies & virtual environments
    - "**/node_modules/**"
    - "**/.venv/**"
    - "**/venv/**"
    - "**/env/**"
    - "**/__pycache__/**"
    - "**/.pytest_cache/**"
    - "**/.egg-info/**"
    - "**/.mypy_cache/**"
    - "**/.ruff_cache/**"
    
    # Build & dist artifacts
    - "**/dist/**"
    - "**/build/**"
    - "**/out/**"
    - "**/.next/**"
    - "**/coverage/**"
    
    # Minified & bundled code
    - "**/*.min.js"
    - "**/*.min.css"
    - "**/bundle.js"
    - "**/*.generated.*"
    
    # Third-party code
    - "**/vendor/**"
    - "**/third_party/**"
    - "**/third-party/**"
    - "**/node_modules-cache/**"
    
    # Test snapshots & fixtures
    - "**/__snapshots__/**"
    - "**/fixtures/**"
    - "**/test/fixtures/**"
    - "**/tests/fixtures/**"
    - "**/*.snap"
