```prompt
---
agent: agent
description: Implement safe runtime overrides without code changes.
---
Task:
Design override precedence and runtime injection mechanism.
Requirements:
Document order: base config, env overrides, runtime overrides.
Constraints:
Avoid ambiguous precedence rules.
Success Criteria:
Overrides are predictable and observable in logs/status.
```
